## Example

```python
import numpy as np


x = np.random.random(1000)
y = x + 0.1*np.random.random(1000)

n =    # chose from saturation of entropy
total_num=len(x) #or len(y)

ex,ey,Nmi = mutual_info(x,y,n,total_num)
print(Nmi)
```
